# QeEdu Docs

Documentation site for QeEdu.

The repository keeps a Mintlify-style `docs.json` navigation file and MDX source files, while also shipping a lightweight Vite frontend so it can be deployed directly to Cloudflare Pages.

## Development

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm build
```

